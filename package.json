{
  "name": "router-page",
  "version": "0.0.0",
  "scripts": {
    "dev": "svite",
    "build": "svite build",
    "deploy": "svite build --base /svelte-pagejs --assetsDir assets && gh-pages -d dist"
  },
  "dependencies": {
    "page": "^1.11.6",
    "svelte": "3.24.1",
    "svelte-hmr": "0.10.3"
  },
  "devDependencies": {
    "svite": "^0.6.0",
    "gh-pages": "^3.1.0"
  }
}